# Tasks

- [ ] Task 1: Mettre à jour la release `latest` dans `releases.json` avec les données de la version 3.1.10 (version, tag, date, githubReleaseUrl, changelog résumé, tous les downloads avec leurs URLs, tailles et SHA256)
- [ ] Task 2: Vider le tableau `history` dans `releases.json`
- [ ] Task 3: Supprimer le fichier `app/versions/page.tsx`
- [ ] Task 4: Retirer le lien "Versions" du tableau `navLinks` dans `app/components/Header.tsx`

# Task Dependencies
- Les Tasks 1 à 4 sont indépendantes et peuvent être exécutées en parallèle.